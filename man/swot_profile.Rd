% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swot_profile.R
\name{swot_profile}
\alias{swot_profile}
\title{Prepare a longitudinal SWOT river profile}
\usage{
swot_profile(
  swot_tibble,
  sword,
  cycle_id = NULL,
  pass_id = NULL,
  time = NULL,
  time_tolerance = 1440,
  variable = NULL,
  quality = c("all", "good", "good_suspect", "usable"),
  distance_col = NULL,
  distance_unit = c("km", "m"),
  plot = FALSE,
  plot_variable = NULL
)
}
\arguments{
\item{swot_tibble}{A tibble containing SWOT RiverSP reach or node
observations. The object must contain either a \code{reach_id} or \code{node_id}
column and a \code{time} column.}

\item{sword}{A data frame, tibble, or \code{sf} object containing the
corresponding SWORD reaches or nodes. It must contain the same identifier
(\code{reach_id} or \code{node_id}) as \code{swot_tibble} and a downstream-distance
variable.}

\item{cycle_id}{Optional SWOT cycle identifier.}

\item{pass_id}{Optional SWOT pass identifier. Usually supplied together
with \code{cycle_id}.}

\item{time}{Optional target time. If supplied, observations within
\code{time_tolerance} of this time are retained.}

\item{time_tolerance}{Maximum temporal difference from \code{time}, in minutes.
Default is 30 minutes.}

\item{variable}{Character vector containing variables to retain, for example
\code{"wse"} or \code{c("wse", "width")}. If \code{NULL}, all variables are retained.}

\item{quality}{Quality classes to retain. One of \code{"all"}, \code{"good"},
\code{"good_suspect"}, or \code{"usable"}. \code{"good"} retains quality flag 0,
\code{"good_suspect"} retains flags 0 and 1, and \code{"usable"} retains flags
0, 1, and 2.}

\item{distance_col}{Name of the SWORD column containing distance to the
outlet. If \code{NULL}, the function attempts to identify a suitable distance
column automatically.}

\item{distance_unit}{Unit of the returned longitudinal distance.
Either \code{"km"} or \code{"m"}.}

\item{plot}{Logical. If \code{TRUE}, return a longitudinal profile plot instead
of the profile tibble. Default is \code{FALSE}.}

\item{plot_variable}{Character string specifying the variable to plot when
\code{plot = TRUE}. If \code{NULL} and exactly one \code{variable} is selected, that
variable is used automatically.}
}
\value{
If \code{plot = FALSE}, a tibble containing SWOT observations ordered
longitudinally from upstream to downstream. The column \code{distance} contains
distance to the outlet in the requested unit.

If \code{plot = TRUE}, a \code{ggplot} object showing the selected variable along
the longitudinal river profile.
}
\description{
Extracts a longitudinal profile from SWOT RiverSP reach or node
observations. The function joins SWOT observations with the corresponding
SWORD nodes or reaches to obtain distance to the outlet, selects observations
from a SWOT cycle/pass or around a specified time, optionally applies
quality filtering, and orders observations longitudinally from upstream
to downstream.

Optionally, the function can return a longitudinal \code{ggplot} for a selected
variable.
}
\examples{
swot <- tibble::tibble(
  node_id = c("1", "2", "3", "4"),
  reach_id = rep("21602300301", 4),
  time = as.POSIXct(
    c(
      "2025-01-12 10:00:01",
      "2025-01-12 10:00:03",
      "2025-01-12 10:00:06",
      "2025-01-12 10:00:08"
    ),
    tz = "UTC"
  ),
  cycle_id = rep(40, 4),
  pass_id = rep(15, 4),
  wse = c(105.2, 104.7, 104.1, 103.5),
  wse_u = c(0.08, 0.07, 0.10, 0.09),
  width = c(180, 195, 210, 225),
  width_u = c(4, 5, 4, 3),
  node_q = c(0, 0, 1, 2)
)

sword <- tibble::tibble(
  node_id = c("1", "2", "3", "4"),
  hydro_dist_out = c(40000, 30000, 20000, 10000)
)

swot_profile(
  swot,
  sword,
  cycle_id = 40,
  pass_id = 15,
  variable = c("wse", "width"),
  quality = "usable"
)

swot_profile(
  swot,
  sword,
  cycle_id = 40,
  pass_id = 15,
  variable = c("wse", "width"),
  quality = "usable",
  plot = TRUE,
  plot_variable = "wse"
)

}
\author{
Florian Betz
}
